/**
 * Socket.io event types
 * Events prefixed by module:
 * - display-*: Display events
 * - content-*: Content events
 * - sync-*: Synchronization events
 * - admin-*: Admin events
 */

// Placeholder - will be populated with Socket.io event types
export {};
